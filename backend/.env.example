# Server Configuration
PORT=3000
ENV=development

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:26257/stocks?sslmode=disable

# External API Configuration
STOCK_API_URL=https://api.karenai.click/swechallenge/list
STOCK_API_KEY=your_api_key_here

# Sync Configuration
# Maximum number of pages to fetch during sync (each page ~10 stocks)
# Default: 20 pages = ~200 stocks. Increase for more data, decrease for faster syncs.
MAX_SYNC_PAGES=20

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
